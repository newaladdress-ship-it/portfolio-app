You are a senior AI + MERN developer. The SmartTalk AI system is already implemented and working. Your task is to UPGRADE it into a professional client-conversion AI assistant.

DO NOT rebuild anything. ONLY enhance existing functionality.

========================================
GOALS
=====

1. Add CLIENT DISCOVERY FLOW (MOST IMPORTANT)
2. Improve AI prompt behavior
3. Capture leads in Firebase
4. Make chatbot act like a real agency assistant

========================================

1. CLIENT DISCOVERY SYSTEM
   ========================================

Enhance the chatbot so it detects user intent.

If user says:

* "I want a website"
* "I need an app"
* "build software"

Then switch into CLIENT CONSULTATION MODE.

In this mode:

* Ask ONE question at a time (not all at once)
* Store answers step-by-step

Questions flow:

Step 1:
"What type of project do you need? (website, mobile app, software)"

Step 2:
"What is the purpose of your project?"

Step 3:
"How many pages or features do you need?"

Step 4:
"Do you need an admin panel or dashboard?"

Step 5:
"Do you have any reference website or app?"

Step 6:
"What is your expected timeline?"

Step 7:
(Optional) "Do you have a budget range?"

After collecting answers:

* Generate a summary
* Show user their requirements
* Suggest contacting developer

========================================
2. FIREBASE LEAD STORAGE
========================

Create a new collection:
"client_leads"

Store:

* user session id
* all answers collected
* timestamp

Each step should update the document progressively.

========================================
3. SMART PROMPT UPGRADE
=======================

Update system prompt:

* Keep portfolio answers strict
* Add client consultation behavior
* Reject unrelated questions

Add logic:

* If message is unrelated → respond:
  "I only assist with my portfolio and project-related queries."

========================================
4. STATE MANAGEMENT
===================

Track:

* Whether user is in consultation mode
* Current step number

Store state:

* In frontend (React state)
* Optionally sync with Firebase

========================================
5. UI IMPROVEMENTS
==================

In sidebar and full page:

* Show progress indicator:
  "Step 2 of 6"

* Show structured responses clearly

* Add final CTA button:
  "Contact Developer"

========================================
6. FINAL BEHAVIOR
=================

The chatbot should:

* Act like a professional agency assistant
* Help user define their project
* Capture leads
* Convert user into potential client

DO NOT break existing UI or features.
ONLY extend functionality cleanly.

========================================

Implement everything in a modular and scalable way.
