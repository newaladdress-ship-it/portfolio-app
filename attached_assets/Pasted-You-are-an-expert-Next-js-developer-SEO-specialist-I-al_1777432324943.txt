You are an expert Next.js developer + SEO specialist.

I already have a working portfolio website built in Next.js with around 10 pages.
Your task is to SCALE this website for SEO rankings using a given keyword list.

IMPORTANT RULES:
- DO NOT change the UI/UX design
- DO NOT break existing pages
- REUSE existing components (Navbar, Footer, Sections, Cards, Layouts)
- Maintain clean, modular structure

-----------------------------------

TASK 1: KEYWORD-BASED PAGE GENERATION

I will provide a list of keywords (low KD 0-20).
For EACH keyword:

1. Create a NEW SEO-optimized page
2. URL slug should be keyword-based
   Example:
   keyword: "bathroom remodeling chandler"
   url: /bathroom-remodeling-chandler

3. Each page must include:
   - Unique H1 (exact keyword)
   - SEO-rich content (800–1500 words)
   - Proper headings (H2, H3)
   - CTA sections (reuse existing components)
   - Internal linking to other pages

-----------------------------------

TASK 2: META SEO OPTIMIZATION

For EACH page:

- Meta Title (60 characters optimized for CTR)
- Meta Description (150-160 characters)
- Open Graph tags
- Canonical URL
- Keyword placement naturally

-----------------------------------

TASK 3: COMPONENT REUSE (IMPORTANT)

- Use existing components instead of creating new ones
- Dynamically pass content via props
- Avoid code duplication
- If needed, create a reusable "SEOContentSection" component

-----------------------------------

TASK 4: DYNAMIC STRUCTURE (VERY IMPORTANT)

Instead of hardcoding everything:

- Create a data-driven system:
  Example:
  /data/keywords.js or keywords.json

Each keyword object should contain:
{
  keyword: "",
  slug: "",
  title: "",
  description: "",
  content: ""
}

Then dynamically generate pages using mapping or dynamic routes:
Example:
pages/[slug].js OR app/[slug]/page.js

-----------------------------------

TASK 5: INTERNAL LINKING

- Each page should link to:
  - 2–3 related keyword pages
  - Homepage
- Improves SEO structure

-----------------------------------

TASK 6: EXISTING PAGES OPTIMIZATION

- Update current 10 pages:
  - Add better content
  - Improve keyword targeting
  - Add internal links
  - Optimize headings

-----------------------------------

TASK 7: PERFORMANCE & SEO BEST PRACTICES

- Use Next.js SEO best practices
- Lazy load components if needed
- Optimize images
- Use semantic HTML
- Ensure fast loading

-----------------------------------

TASK 8: SCALABILITY

System should support:
- 100+ pages easily
- Future keyword addition without redesign

-----------------------------------

FINAL GOAL:

Transform this small portfolio site into a large SEO-optimized content site
that can rank for multiple low-competition keywords without changing design.

Wait for my keyword list and then start generating pages.