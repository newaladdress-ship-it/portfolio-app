You are a senior MERN + Firebase developer. I already have a working website deployed on Vercel with Firebase configured. Your task is to ADD a fully functional AI chatbot feature into the existing project WITHOUT breaking or modifying the current UI design, layout, or styling.

IMPORTANT RULES:

* Do NOT modify any existing components, pages, or global styles.
* All new code must be modular and isolated.
* The chatbot UI must NOT interfere with the current website layout.
* Use clean, production-level code structure.
* Use environment variables for API keys.
* Keep everything scalable and reusable.

========================================
FEATURE REQUIREMENTS
====================

1. CREATE A NEW PAGE:

* Create a new route/page: "/ai-assistant"
* This page will be used for testing and showcasing the chatbot
* The page should include:

  * A clean chat interface
  * Chat messages (user on right, bot on left)
  * Input box + send button
  * Scrollable message container

---

2. FLOATING CHATBOT WIDGET (MAIN FEATURE):

* Add a floating chatbot icon at the bottom-right corner of the website
* This icon must appear on ALL pages globally
* When clicked:
  → A sidebar panel should slide in from the right

---

3. SIDEBAR DESIGN:

* Width: 25% of screen (approx 1/4 screen)
* Position: fixed, right side
* Height: 100%
* Background: clean modern (white or dark based on site theme)
* Smooth slide-in animation

---

4. SIDEBAR CONTENT:

Inside the sidebar:

* Header:

  * Title: "AI Assistant"
  * Close/Toggle button (X icon)

* Chat area:

  * Scrollable messages
  * User messages aligned right
  * Bot messages aligned left

* Input section:

  * Text input field
  * Send button

* Add initial auto message:
  "Hi! I can help you explore my projects and services."

---

5. TOGGLE FUNCTIONALITY:

* Clicking chatbot icon → OPEN sidebar
* Clicking close (X) → CLOSE sidebar
* Sidebar should NOT reload page
* Use React state for control

---

6. BACKEND / AI INTEGRATION:

* Create an API route (Node.js or serverless function)

* Connect with OpenAI API using environment variable:
  process.env.OPENAI_API_KEY

* Use model: "gpt-4.1-mini"

---

7. CONTROLLED AI BEHAVIOR (VERY IMPORTANT):

The chatbot must ONLY answer questions related to portfolio.

Use this prompt:

"You are a professional AI assistant for a developer portfolio.

Rules:

* Answer ONLY using the provided portfolio data
* If question is unrelated, respond:
  'I only answer questions related to my portfolio and services.'
* Keep answers short, professional, and helpful
* Encourage user to contact when relevant"

---

8. FIREBASE USAGE:

Firebase is already configured. Use it for:

* Storing chat history (Firestore collection: "chat_logs")
* Each message should store:

  * user message
  * bot response
  * timestamp

Do NOT reconfigure Firebase, just use existing setup.

---

9. PORTFOLIO DATA (STATIC CONTEXT):

Create a separate file:

portfolioData.js

Include:

* Skills
* Projects
* Services
* Contact info

Inject this data into AI prompt as context.

---

10. FRONTEND STRUCTURE:

Create components:

* ChatWidget.jsx (floating icon + toggle)
* ChatSidebar.jsx (main chat panel)
* ChatMessage.jsx (message bubble)
* ChatInput.jsx (input + send button)

Keep everything modular.

---

11. UI/UX REQUIREMENTS:

* Use Tailwind CSS (or existing styling system)
* Smooth animations (transition, ease-in-out)
* Responsive design
* Clean modern chat bubbles
* Add typing indicator ("AI is typing...")

---

12. PERFORMANCE:

* Prevent empty messages
* Handle API errors gracefully
* Show loading state while waiting for response

---

13. FINAL GOAL:

The chatbot should:

* Work on all pages
* Not break existing UI
* Look like a professional SaaS feature
* Answer only portfolio-related questions
* Be scalable for future client projects

========================================

Now implement ALL of the above in a clean, production-ready way.
Do not skip any step.
Keep code well-structured and maintainable.
